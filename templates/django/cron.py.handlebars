from django_cron import CronJobBase, Schedule

from .models import {{#list models}} {{model.singularCapitalized}},{{/list}} _random_id


class {{name}}CronJob(CronJobBase):
    RUN_AT_TIMES = ['0:00',]
    schedule = Schedule(run_at_times=RUN_AT_TIMES)
    code = '{{id}}.entropy'

    def do(self):
        pass
